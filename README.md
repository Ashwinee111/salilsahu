# Salil Sahu — exact screenshot recreation

React + Tailwind CSS/Vite single-page responsive implementation based on the supplied screenshot.

## Run
npm install
npm run dev

## Build
npm run build

The image assets in `public/images` are crops from the supplied screenshot for visual fidelity. Replace them with the original high-resolution assets when available; the layout and paths are already wired.
